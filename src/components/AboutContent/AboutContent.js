import React    from "react";
import template from "./AboutContent.jsx";

class AboutContent extends React.Component {
  render() {
    return template.call(this);
  }
}

export default AboutContent;
